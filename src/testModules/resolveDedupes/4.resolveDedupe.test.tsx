import {TestFilters} from "../shared/models/test.models";

const filters:TestFilters = {
    operatingUnit: 'Botswana',
    dataType: 'MER Results',
    period: 'Oct - Dec 2020',
    includeResolved: true
};

test(`4 > Resolve Dedupes > Botswana`, ()=>{

});